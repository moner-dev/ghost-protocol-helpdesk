/**
 * GHOST PROTOCOL — useTickets Hook
 *
 * Alias for useIncidents — maintains backward compatibility
 * with any code referencing "tickets" instead of "incidents".
 */

export { useIncidents as useTickets, useIncidents as default } from './useIncidents';
